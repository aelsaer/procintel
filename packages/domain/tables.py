"""SQLAlchemy Core table definitions mirroring db/migrations/*.sql.

Only the tables the ΚΗΜΔΗΣ connector (all five resources + adamChain/process
grouping) touches are defined here — more get added as later
connectors/slices need them (db/migrations remains the source of truth for
DDL; this module is query-building metadata only, it never calls
`metadata.create_all()`). Column names/types are kept in sync by hand with
db/migrations/01_extensions_and_source_layer.sql, 02_identity_and_registry.sql
and 03_procurement_core.sql — if you change one, change the other.

`act_locations.geom` (PostGIS geometry) is still intentionally omitted:
the ΚΗΜΔΗΣ connector never writes it (contract records don't carry
coordinates). `administrative_boundaries.geom` *is* modeled, using
GeoAlchemy2's `Geometry` type — the CKAN connector's boundaries adapter
(`services/ingestion/connectors/ckan/boundaries.py`) is the first slice
that actually needs to write geometry.
"""

from __future__ import annotations

import sqlalchemy as sa
from geoalchemy2 import Geometry
from sqlalchemy.dialects.postgresql import JSONB, UUID

metadata = sa.MetaData()

source_records = sa.Table(
    "source_records",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("source_system", sa.Text, nullable=False),
    sa.Column("resource_type", sa.Text, nullable=False),
    sa.Column("source_native_id", sa.Text),
    sa.Column("source_version", sa.Text),
    sa.Column("content_sha256", sa.Text, nullable=False),
    sa.Column("payload_uri", sa.Text, nullable=False),
    sa.Column("fetched_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("source_updated_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("http_status", sa.Integer),
    sa.Column("schema_version", sa.Text),
    sa.Column("license_code", sa.Text, nullable=False),
    sa.Column("attribution_text", sa.Text, nullable=False),
    sa.Column("request_url_hash", sa.Text),
    sa.Column("parse_status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("parse_error", JSONB),
    sa.Column("is_latest", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

diavgeia_organizations = sa.Table(
    "diavgeia_organizations",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("uid", sa.Text, nullable=False, unique=True),
    sa.Column("label", sa.Text, nullable=False),
    sa.Column("abbreviation", sa.Text),
    sa.Column("category", sa.Text),
    sa.Column("active", sa.Boolean),
    sa.Column("vat_number", sa.Text),
    sa.Column("website", sa.Text),
    sa.Column("email", sa.Text),
    sa.Column("raw", JSONB, nullable=False, server_default="{}"),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

diavgeia_units = sa.Table(
    "diavgeia_units",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("uid", sa.Text, nullable=False, unique=True),
    sa.Column("organization_uid", sa.Text, nullable=False),
    sa.Column("label", sa.Text, nullable=False),
    sa.Column("category", sa.Text),
    sa.Column("active", sa.Boolean),
    sa.Column("active_from", sa.TIMESTAMP(timezone=True)),
    sa.Column("active_until", sa.TIMESTAMP(timezone=True)),
    sa.Column("parent_uid", sa.Text),
    sa.Column("unit_domains", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("raw", JSONB, nullable=False, server_default="{}"),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

diavgeia_signers = sa.Table(
    "diavgeia_signers",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("uid", sa.Text, nullable=False),
    sa.Column("organization_uid", sa.Text),
    sa.Column("first_name", sa.Text),
    sa.Column("last_name", sa.Text),
    sa.Column("active", sa.Boolean),
    sa.Column("active_from", sa.TIMESTAMP(timezone=True)),
    sa.Column("active_until", sa.TIMESTAMP(timezone=True)),
    sa.Column("raw", JSONB, nullable=False, server_default="{}"),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

diavgeia_decision_versions = sa.Table(
    "diavgeia_decision_versions",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("ada", sa.Text),
    sa.Column("version_id", sa.Text, nullable=False, unique=True),
    sa.Column("corrected_version_id", sa.Text),
    sa.Column("status", sa.Text),
    sa.Column("submission_timestamp", sa.TIMESTAMP(timezone=True)),
    sa.Column("issue_date", sa.TIMESTAMP(timezone=True)),
    sa.Column("document_url", sa.Text),
    sa.Column("document_checksum", sa.Text),
    sa.Column("organization_uid", sa.Text),
    sa.Column("unit_uids", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("signer_uids", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("raw", JSONB, nullable=False, server_default="{}"),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

connector_runs = sa.Table(
    "connector_runs",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("source_system", sa.Text, nullable=False),
    sa.Column("resource_type", sa.Text, nullable=False),
    sa.Column("partition_key", sa.Text, nullable=False),
    sa.Column("run_type", sa.Text, nullable=False),
    sa.Column("status", sa.Text, nullable=False, server_default="RUNNING"),
    sa.Column("pages_fetched", sa.Integer, nullable=False, server_default="0"),
    sa.Column("records_fetched", sa.Integer, nullable=False, server_default="0"),
    sa.Column("records_upserted", sa.Integer, nullable=False, server_default="0"),
    sa.Column("records_unchanged", sa.Integer, nullable=False, server_default="0"),
    sa.Column("records_failed", sa.Integer, nullable=False, server_default="0"),
    sa.Column("enrichment_succeeded", sa.Integer, nullable=False, server_default="0"),
    sa.Column("enrichment_failed", sa.Integer, nullable=False, server_default="0"),
    sa.Column("enrichment_deferred", sa.Integer, nullable=False, server_default="0"),
    sa.Column("metrics", JSONB, nullable=False, server_default="{}"),
    sa.Column("rate_limit_hits", sa.Integer, nullable=False, server_default="0"),
    sa.Column("started_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("finished_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("error", JSONB),
    sa.Column("triggered_by", sa.Text),
)

fetch_requests = sa.Table(
    "fetch_requests",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("identifier_raw", sa.Text, nullable=False),
    sa.Column("identifier_normalized", sa.Text, nullable=False),
    sa.Column("identifier_scheme", sa.Text, nullable=False),
    sa.Column("source_system", sa.Text, nullable=False),
    sa.Column("status", sa.Text, nullable=False, server_default="QUEUED"),
    sa.Column("message", sa.Text),
    sa.Column("result_act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id")),
    sa.Column("result_process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id")),
    sa.Column("requested_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("started_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("finished_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("last_attempt_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("attempt_count", sa.Integer, nullable=False, server_default="0"),
    sa.Column("next_retry_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("request_metadata", JSONB, nullable=False, server_default=sa.text("'{}'::jsonb")),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

source_cursors = sa.Table(
    "source_cursors",
    metadata,
    sa.Column("source_system", sa.Text, primary_key=True),
    sa.Column("resource_type", sa.Text, primary_key=True),
    sa.Column("partition_key", sa.Text, primary_key=True),
    sa.Column("cursor_value", JSONB, nullable=False),
    sa.Column("last_success_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("last_attempt_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("last_error", JSONB),
)

enrichment_jobs = sa.Table(
    "enrichment_jobs",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("provider", sa.Text, nullable=False),
    sa.Column("idempotency_key", sa.Text, nullable=False),
    sa.Column("object_type", sa.Text),
    sa.Column("object_id", UUID(as_uuid=True)),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("payload", JSONB, nullable=False, server_default="{}"),
    sa.Column("status", sa.Text, nullable=False, server_default="QUEUED"),
    sa.Column("priority", sa.SmallInteger, nullable=False, server_default="100"),
    sa.Column("attempt_count", sa.Integer, nullable=False, server_default="0"),
    sa.Column("max_attempts", sa.Integer, nullable=False, server_default="8"),
    sa.Column("available_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("locked_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("locked_by", sa.Text),
    sa.Column("last_error", JSONB),
    sa.Column("result", JSONB),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("finished_at", sa.TIMESTAMP(timezone=True)),
)

data_quality_issues = sa.Table(
    "data_quality_issues",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("object_type", sa.Text),
    sa.Column("object_id", UUID(as_uuid=True)),
    sa.Column("issue_code", sa.Text, nullable=False),
    sa.Column("severity", sa.Text, nullable=False),
    sa.Column("details", JSONB),
    sa.Column("status", sa.Text, nullable=False, server_default="OPEN"),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("resolved_at", sa.TIMESTAMP(timezone=True)),
)

external_datasets = sa.Table(
    "external_datasets",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("catalog_source", sa.Text, nullable=False),
    sa.Column("catalog_dataset_id", sa.Text, nullable=False),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("publisher", sa.Text),
    sa.Column("license_code", sa.Text),
    sa.Column("resource_type", sa.Text),
    sa.Column("resource_url", sa.Text),
    sa.Column("update_frequency", sa.Text),
    sa.Column("last_seen_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("ingestion_status", sa.Text, nullable=False, server_default="NOT_ONBOARDED"),
    sa.Column("adapter_name", sa.Text),
    sa.Column("config", JSONB),
)

external_dataset_validations = sa.Table(
    "external_dataset_validations",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column(
        "external_dataset_id",
        UUID(as_uuid=True),
        sa.ForeignKey("external_datasets.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("adapter_name", sa.Text, nullable=False),
    sa.Column("resource_url", sa.Text, nullable=False),
    sa.Column("schema_fingerprint", sa.Text, nullable=False),
    sa.Column("detected_format", sa.Text),
    sa.Column("columns", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("sample", JSONB),
    sa.Column("status", sa.Text, nullable=False),
    sa.Column("errors", JSONB, nullable=False, server_default="[]"),
    sa.Column("validated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

spatial_service_capabilities = sa.Table(
    "spatial_service_capabilities",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("catalog_source", sa.Text, nullable=False),
    sa.Column("service_url", sa.Text, nullable=False),
    sa.Column("service_type", sa.Text, nullable=False),
    sa.Column("service_version", sa.Text),
    sa.Column("title", sa.Text),
    sa.Column("provider_name", sa.Text),
    sa.Column("access_constraints", sa.Text),
    sa.Column("fees", sa.Text),
    sa.Column("formats", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("layers", JSONB, nullable=False, server_default="[]"),
    sa.Column("status", sa.Text, nullable=False),
    sa.Column("http_status", sa.Integer),
    sa.Column("content_sha256", sa.Text),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("last_error", JSONB),
    sa.Column("checked_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("last_available_at", sa.TIMESTAMP(timezone=True)),
    sa.UniqueConstraint("service_url", "service_type", name="uq_spatial_capability_url_type"),
)

nuts_areas = sa.Table(
    "nuts_areas",
    metadata,
    sa.Column("code", sa.Text, primary_key=True),
    sa.Column("level", sa.SmallInteger, nullable=False),
    sa.Column("name_el", sa.Text),
    sa.Column("name_en", sa.Text),
    sa.Column("classification_version", sa.Text, nullable=False),
    sa.Column("parent_code", sa.Text, sa.ForeignKey("nuts_areas.code")),
    sa.Column("geom", Geometry(geometry_type="MULTIPOLYGON", srid=4326)),
)

postal_code_nuts = sa.Table(
    "postal_code_nuts",
    metadata,
    sa.Column("country_code", sa.CHAR(2), primary_key=True),
    sa.Column("postal_code", sa.Text, primary_key=True),
    sa.Column("nuts_code", sa.Text, primary_key=True),
    sa.Column("classification_version", sa.Text, primary_key=True),
    sa.Column("source_record_id", UUID(as_uuid=True), nullable=False),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False),
)

# geo_denominators.nuts_code is a soft reference to nuts_areas(code) — that
# reference table isn't modeled here yet (nothing in this pass writes to
# it), same "looked up by, not hard-FK'd from" convention already used by
# act_locations.nuts_code.
geo_denominators = sa.Table(
    "geo_denominators",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("metric_name", sa.Text, nullable=False),
    sa.Column("nuts_code", sa.Text),
    sa.Column("municipality_code", sa.Text),
    sa.Column("reference_year", sa.Integer, nullable=False),
    sa.Column("value", sa.Numeric(20, 2), nullable=False),
    sa.Column("external_dataset_id", UUID(as_uuid=True), sa.ForeignKey("external_datasets.id")),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
)

# administrative_boundaries.nuts_code is a soft reference to nuts_areas(code)
# — not modeled here, same "looked up by, not hard-FK'd from" convention as
# act_locations.nuts_code (nuts_areas itself isn't written by anything yet).
administrative_boundaries = sa.Table(
    "administrative_boundaries",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("boundary_type", sa.Text, nullable=False),
    sa.Column("external_dataset_id", UUID(as_uuid=True), sa.ForeignKey("external_datasets.id")),
    sa.Column("code", sa.Text),
    sa.Column("name", sa.Text),
    sa.Column("nuts_code", sa.Text),
    sa.Column("geom", Geometry(geometry_type="MULTIPOLYGON", srid=4326), nullable=False),
    sa.Column("valid_from", sa.TIMESTAMP(timezone=True)),
    sa.Column("valid_to", sa.TIMESTAMP(timezone=True)),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
)

facilities = sa.Table(
    "facilities",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("facility_type", sa.Text, nullable=False),
    sa.Column("external_dataset_id", UUID(as_uuid=True), sa.ForeignKey("external_datasets.id")),
    sa.Column("code", sa.Text),
    sa.Column("name", sa.Text),
    sa.Column("nuts_code", sa.Text),
    sa.Column("municipality_code", sa.Text),
    sa.Column("capacity_metric", sa.Text),
    sa.Column("capacity_value", sa.Numeric(12, 0)),
    sa.Column("geom", Geometry(geometry_type="POINT", srid=4326)),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
)

entities = sa.Table(
    "entities",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_type", sa.Text, nullable=False),
    sa.Column("canonical_name", sa.Text, nullable=False),
    sa.Column("normalized_name", sa.Text, nullable=False),
    sa.Column("country_code", sa.CHAR(2)),
    sa.Column("status", sa.Text, nullable=False, server_default="ACTIVE"),
    sa.Column("merged_into_id", UUID(as_uuid=True), sa.ForeignKey("entities.id")),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

entity_identifiers = sa.Table(
    "entity_identifiers",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("scheme", sa.Text, nullable=False),
    sa.Column("value_raw", sa.Text, nullable=False),
    sa.Column("value_normalized", sa.Text, nullable=False),
    sa.Column("country_code", sa.CHAR(2)),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False, server_default="1"),
    sa.Column("identifier_valid", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("match_eligibility", sa.Text, nullable=False, server_default="ELIGIBLE"),
    sa.Column("valid_from", sa.TIMESTAMP(timezone=True)),
    sa.Column("valid_to", sa.TIMESTAMP(timezone=True)),
    sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

entity_names = sa.Table(
    "entity_names",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("normalized_name", sa.Text, nullable=False),
    sa.Column("name_search", sa.Text),
    sa.Column("name_without_legal_form", sa.Text),
    sa.Column("name_type", sa.Text, nullable=False),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("valid_from", sa.TIMESTAMP(timezone=True)),
    sa.Column("valid_to", sa.TIMESTAMP(timezone=True)),
    sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.true()),
)

entity_addresses = sa.Table(
    "entity_addresses",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("address_line", sa.Text),
    sa.Column("postal_code", sa.Text),
    sa.Column("municipality", sa.Text),
    sa.Column("region", sa.Text),
    sa.Column("nuts_code", sa.Text),
    sa.Column("country_code", sa.CHAR(2)),
    sa.Column("geom", Geometry(geometry_type="POINT", srid=4326)),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("valid_from", sa.TIMESTAMP(timezone=True)),
    sa.Column("valid_to", sa.TIMESTAMP(timezone=True)),
    sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.true()),
)

entity_contacts = sa.Table(
    "entity_contacts",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("contact_type", sa.Text, nullable=False),
    sa.Column("value_raw", sa.Text, nullable=False),
    sa.Column("value_normalized", sa.Text, nullable=False),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False, server_default="1"),
    sa.Column("valid_from", sa.TIMESTAMP(timezone=True)),
    sa.Column("valid_to", sa.TIMESTAMP(timezone=True)),
    sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

entity_match_candidates = sa.Table(
    "entity_match_candidates",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_a_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("entity_b_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("score", sa.Numeric(5, 4), nullable=False),
    sa.Column("score_breakdown", JSONB, nullable=False),
    sa.Column("blocking_reason", sa.Text, nullable=False),
    sa.Column("status", sa.Text, nullable=False, server_default="PENDING_REVIEW"),
    sa.Column("reviewed_by", UUID(as_uuid=True)),
    sa.Column("reviewed_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("review_notes", sa.Text),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

entity_merge_log = sa.Table(
    "entity_merge_log",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("surviving_entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("merged_entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("match_candidate_id", UUID(as_uuid=True), sa.ForeignKey("entity_match_candidates.id")),
    sa.Column("merge_reason", sa.Text, nullable=False),
    sa.Column("evidence", JSONB, nullable=False),
    sa.Column("performed_by", sa.Text, nullable=False),
    sa.Column("performed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("reverted_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("reverted_by", sa.Text),
)

entity_company_snapshots = sa.Table(
    "entity_company_snapshots",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("official_name", sa.Text),
    sa.Column("trade_name", sa.Text),
    sa.Column("gemi_number", sa.Text),
    sa.Column("vat_number", sa.Text),
    sa.Column("legal_form", sa.Text),
    sa.Column("legal_form_code", sa.Text),
    sa.Column("company_status", sa.Text),
    sa.Column("gemi_office", sa.Text),
    sa.Column("gemi_registration_date", sa.Date),
    sa.Column("kad_codes", sa.ARRAY(sa.Text)),
    sa.Column("municipality", sa.Text),
    sa.Column("region", sa.Text),
    sa.Column("address_line", sa.Text),
    sa.Column("postal_code", sa.Text),
    sa.Column("city", sa.Text),
    sa.Column("email", sa.Text),
    sa.Column("website", sa.Text),
    sa.Column("objective", sa.Text),
    sa.Column("last_status_change", sa.Date),
    sa.Column("is_branch", sa.Boolean),
    sa.Column("auto_registered", sa.Boolean),
    sa.Column("kad_details", JSONB, nullable=False, server_default="[]"),
    sa.Column("persons", JSONB, nullable=False, server_default="[]"),
    sa.Column("capital", JSONB),
    sa.Column("source_documents", JSONB, nullable=False, server_default="[]"),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("valid_from", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("valid_to", sa.TIMESTAMP(timezone=True)),
    sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

entity_vies_checks = sa.Table(
    "entity_vies_checks",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("country_code", sa.CHAR(2), nullable=False),
    sa.Column("national_number", sa.Text, nullable=False),
    sa.Column("normalized_eu_vat", sa.Text, nullable=False),
    sa.Column("checked_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("vies_valid", sa.Boolean),
    sa.Column("vies_response_hash", sa.Text),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

procurement_processes = sa.Table(
    "procurement_processes",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("public_id", sa.Text, nullable=False),
    sa.Column("title", sa.Text),
    sa.Column("normalized_title", sa.Text),
    sa.Column("lifecycle_status", sa.Text, nullable=False, server_default="DISCOVERED"),
    sa.Column("record_status", sa.Text, nullable=False, server_default="ACTIVE"),
    sa.Column("merged_into_process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id")),
    sa.Column("buyer_entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id")),
    sa.Column("primary_cpv_code", sa.Text),
    sa.Column("estimated_value", sa.Numeric(20, 2)),
    sa.Column("awarded_value", sa.Numeric(20, 2)),
    sa.Column("current_contract_value", sa.Numeric(20, 2)),
    sa.Column("currency", sa.CHAR(3), server_default="EUR"),
    sa.Column("first_observed_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("last_observed_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

procurement_acts = sa.Table(
    "procurement_acts",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id")),
    sa.Column("act_type", sa.Text, nullable=False),
    sa.Column("title", sa.Text),
    sa.Column("normalized_title", sa.Text),
    sa.Column("publication_date", sa.Date),
    sa.Column("submission_date", sa.Date),
    sa.Column("submission_deadline", sa.TIMESTAMP(timezone=True)),
    sa.Column("decision_date", sa.Date),
    sa.Column("start_date", sa.Date),
    sa.Column("end_date", sa.Date),
    sa.Column("status", sa.Text),
    sa.Column("amount_net", sa.Numeric(20, 2)),
    sa.Column("vat_amount", sa.Numeric(20, 2)),
    sa.Column("amount_gross", sa.Numeric(20, 2)),
    sa.Column("currency", sa.CHAR(3), server_default="EUR"),
    sa.Column("procedure_type", sa.Text),
    sa.Column("agreement_type", sa.Text, nullable=False, server_default="STANDARD"),
    sa.Column("framework_ceiling_amount", sa.Numeric(20, 2)),
    sa.Column("source_details", JSONB, nullable=False, server_default=sa.text("'{}'::jsonb")),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id"), nullable=False),
    sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

act_identifiers = sa.Table(
    "act_identifiers",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False),
    sa.Column("scheme", sa.Text, nullable=False),
    sa.Column("value_raw", sa.Text, nullable=False),
    sa.Column("value_normalized", sa.Text, nullable=False),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False, server_default="1"),
)

act_parties = sa.Table(
    "act_parties",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("party_role", sa.Text, nullable=False),
    sa.Column("lot_id", UUID(as_uuid=True)),  # FK -> procurement_lots, not defined in this slice
    sa.Column("amount", sa.Numeric(20, 2)),
    sa.Column("currency", sa.CHAR(3), server_default="EUR"),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
)

act_cpv_codes = sa.Table(
    "act_cpv_codes",
    metadata,
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), primary_key=True),
    sa.Column("cpv_code", sa.Text, primary_key=True),
    sa.Column("lot_id", UUID(as_uuid=True)),  # FK -> procurement_lots, not defined in this slice
    sa.Column("is_primary", sa.Boolean, nullable=False, server_default=sa.false()),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
)

act_locations = sa.Table(
    "act_locations",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False),
    sa.Column("nuts_code", sa.Text),
    sa.Column("municipality_code", sa.Text),
    sa.Column("postal_code", sa.Text),
    sa.Column("place_text", sa.Text),
    sa.Column("municipality_name", sa.Text),
    sa.Column("regional_unit_name", sa.Text),
    sa.Column("region_name", sa.Text),
    sa.Column("country_code", sa.CHAR(2)),
    sa.Column("location_kind", sa.Text, nullable=False, server_default="PERFORMANCE"),
    sa.Column("granularity", sa.Text),
    sa.Column("extraction_method", sa.Text),
    sa.Column("geocode_provider", sa.Text),
    sa.Column("confidence", sa.Numeric(5, 4)),
    sa.Column("evidence", JSONB, nullable=False, server_default=sa.text("'{}'::jsonb")),
    sa.Column("enrichment_job_id", UUID(as_uuid=True)),
    sa.Column("enriched_at", sa.TIMESTAMP(timezone=True)),
    # geom (PostGIS geometry) intentionally omitted, see module docstring.
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
)

geospatial_enrichment_jobs = sa.Table(
    "geospatial_enrichment_jobs",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id"), nullable=False),
    sa.Column("status", sa.Text, nullable=False, server_default="QUEUED"),
    sa.Column("attempt_count", sa.Integer, nullable=False, server_default="0"),
    sa.Column("available_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("locked_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("locked_by", sa.Text),
    sa.Column("last_error", JSONB),
    sa.Column("result", JSONB),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("finished_at", sa.TIMESTAMP(timezone=True)),
)

geocoding_cache = sa.Table(
    "geocoding_cache",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("provider", sa.Text, nullable=False),
    sa.Column("query_hash", sa.Text, nullable=False),
    sa.Column("query_normalized", sa.Text, nullable=False),
    sa.Column("status", sa.Text, nullable=False),
    sa.Column("latitude", sa.Float),
    sa.Column("longitude", sa.Float),
    sa.Column("display_name", sa.Text),
    sa.Column("municipality_name", sa.Text),
    sa.Column("regional_unit_name", sa.Text),
    sa.Column("region_name", sa.Text),
    sa.Column("postal_code", sa.Text),
    sa.Column("country_code", sa.CHAR(2)),
    sa.Column("precision", sa.Text),
    sa.Column("raw_response", JSONB),
    sa.Column("hit_count", sa.Integer, nullable=False, server_default="0"),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("last_used_at", sa.TIMESTAMP(timezone=True), nullable=False),
)

geocoding_places = sa.Table(
    "geocoding_places",
    metadata,
    sa.Column("geoname_id", sa.BigInteger, primary_key=True),
    sa.Column("country_code", sa.CHAR(2), nullable=False),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("normalized_names", sa.ARRAY(sa.Text), nullable=False),
    sa.Column("admin_name_1", sa.Text),
    sa.Column("admin_code_1", sa.Text),
    sa.Column("admin_name_2", sa.Text),
    sa.Column("admin_code_2", sa.Text),
    sa.Column("admin_name_3", sa.Text),
    sa.Column("admin_code_3", sa.Text),
    sa.Column("latitude", sa.Float, nullable=False),
    sa.Column("longitude", sa.Float, nullable=False),
    sa.Column("feature_class", sa.CHAR(1), nullable=False),
    sa.Column("feature_code", sa.Text, nullable=False),
    sa.Column("population", sa.BigInteger),
    sa.Column("source_name", sa.Text, nullable=False),
    sa.Column("source_version", sa.Text, nullable=False),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False),
)

process_participations = sa.Table(
    "process_participations",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id"), nullable=False),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id")),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id")),
    sa.Column("participant_name_raw", sa.Text),
    sa.Column("participant_afm_raw", sa.Text),
    sa.Column("participation_role", sa.Text, nullable=False),
    sa.Column("evidence_type", sa.Text, nullable=False),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("document_id", UUID(as_uuid=True), sa.ForeignKey("documents.id")),
    sa.Column("source_page", sa.Integer),
    sa.Column("evidence", JSONB, nullable=False, server_default=sa.text("'{}'::jsonb")),
    sa.Column("evidence_key", sa.Text, nullable=False),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

# documents/field_provenance mirror db/migrations/04_documents_and_provenance.sql
# (services/documents, spec §23/§24). document_pages mirrors the later
# 13_document_pages.sql — full-text search over per-page OCR/text-layer
# output, not modeled in 04_* because that migration predates page-level
# text storage being needed.
documents = sa.Table(
    "documents",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id")),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("document_type", sa.Text),
    sa.Column("title", sa.Text),
    sa.Column("source_url", sa.Text),
    sa.Column("object_uri", sa.Text, nullable=False),
    sa.Column("mime_type", sa.Text),
    sa.Column("file_size", sa.BigInteger),
    sa.Column("sha256", sa.Text, nullable=False),
    sa.Column("text_extraction_status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("page_count", sa.Integer),
    sa.Column("language", sa.Text),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

document_act_links = sa.Table(
    "document_act_links",
    metadata,
    sa.Column(
        "document_id",
        UUID(as_uuid=True),
        sa.ForeignKey("documents.id", ondelete="CASCADE"),
        primary_key=True,
    ),
    sa.Column(
        "act_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_acts.id", ondelete="CASCADE"),
        primary_key=True,
    ),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("document_type", sa.Text),
    sa.Column("title", sa.Text),
    sa.Column("source_url", sa.Text),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

field_provenance = sa.Table(
    "field_provenance",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("object_type", sa.Text, nullable=False),
    sa.Column("object_id", UUID(as_uuid=True), nullable=False),
    sa.Column("field_name", sa.Text, nullable=False),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id"), nullable=False),
    sa.Column("source_path", sa.Text),
    sa.Column("extraction_method", sa.Text, nullable=False),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("value_hash", sa.Text),
)

document_pages = sa.Table(
    "document_pages",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("document_id", UUID(as_uuid=True), sa.ForeignKey("documents.id"), nullable=False),
    sa.Column("page_number", sa.Integer, nullable=False),
    sa.Column("text", sa.Text, nullable=False, server_default=""),
    # text_search (generated tsvector column) intentionally omitted — never
    # written to from Python, only queried; same convention as act_locations.geom.
    sa.Column("extraction_method", sa.Text, nullable=False),
    sa.Column("ocr_mean_confidence", sa.Numeric(5, 2)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

document_compliance_fields = sa.Table(
    "document_compliance_fields",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column(
        "process_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_processes.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column(
        "document_id",
        UUID(as_uuid=True),
        sa.ForeignKey("documents.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("page_number", sa.Integer),
    sa.Column("category", sa.Text, nullable=False),
    sa.Column("field_name", sa.Text, nullable=False),
    sa.Column("value", JSONB, nullable=False),
    sa.Column("source_excerpt", sa.Text),
    sa.Column("extraction_method", sa.Text, nullable=False),
    sa.Column("parser_version", sa.Text, nullable=False),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

document_comparisons = sa.Table(
    "document_comparisons",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column(
        "process_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_processes.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column(
        "base_document_id",
        UUID(as_uuid=True),
        sa.ForeignKey("documents.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column(
        "comparison_document_id",
        UUID(as_uuid=True),
        sa.ForeignKey("documents.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("comparison_type", sa.Text, nullable=False),
    sa.Column("summary", sa.Text, nullable=False),
    sa.Column("changes", JSONB, nullable=False),
    sa.Column("parser_version", sa.Text, nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

funding_projects = sa.Table(
    "funding_projects",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("mis_ops_code", sa.Text),
    sa.Column("program_code", sa.Text),
    sa.Column("program_period", sa.Text),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("beneficiary_id", UUID(as_uuid=True), sa.ForeignKey("entities.id")),
    sa.Column("budget", sa.Numeric(20, 2)),
    sa.Column("contracted_amount", sa.Numeric(20, 2)),
    sa.Column("paid_amount", sa.Numeric(20, 2)),
    sa.Column("total_budget", sa.Numeric(20, 2)),
    sa.Column("completion", sa.Numeric(7, 2)),
    sa.Column("absorption", sa.Numeric(7, 2)),
    sa.Column("beneficiary_name_raw", sa.Text),
    sa.Column("description", sa.Text),
    sa.Column("program_title", sa.Text),
    sa.Column("is_state_aid", sa.Boolean),
    sa.Column("is_major", sa.Boolean),
    sa.Column("status_report", sa.Text),
    sa.Column("status_report_date", sa.TIMESTAMP(timezone=True)),
    sa.Column("funds", sa.Text),
    sa.Column("spatial", sa.Text),
    sa.Column("thematic", sa.Text),
    sa.Column("map_kml", sa.Text),
    sa.Column("details", JSONB, nullable=False, server_default="{}"),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("currency", sa.CHAR(3), server_default="EUR"),
    sa.Column("start_date", sa.Date),
    sa.Column("end_date", sa.Date),
    sa.Column("status", sa.Text),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
)

funding_subprojects = sa.Table(
    "funding_subprojects",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("funding_project_id", UUID(as_uuid=True), sa.ForeignKey("funding_projects.id"), nullable=False),
    sa.Column("subproject_index", sa.Integer, nullable=False),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("implementors", JSONB),
    sa.Column("budget", sa.Numeric(20, 2)),
    sa.Column("paid_amount", sa.Numeric(20, 2)),
    sa.Column("completion", sa.Numeric(7, 2)),
    sa.Column("start_date", sa.Date),
    sa.Column("end_date", sa.Date),
    sa.Column("subproject_type", sa.Text),
    sa.Column("is_grant", sa.Boolean),
    sa.Column("estimated_status", JSONB, nullable=False, server_default="{}"),
    sa.Column("actual_status", JSONB, nullable=False, server_default="{}"),
    sa.Column("details", JSONB, nullable=False, server_default="{}"),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

funding_payment_snapshots = sa.Table(
    "funding_payment_snapshots",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("funding_project_id", UUID(as_uuid=True), sa.ForeignKey("funding_projects.id"), nullable=False),
    sa.Column("funding_subproject_id", UUID(as_uuid=True), sa.ForeignKey("funding_subprojects.id")),
    sa.Column("amount", sa.Numeric(20, 2), nullable=False),
    sa.Column("payment_scope", sa.Text, nullable=False),
    sa.Column("reference_date", sa.Date),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

funding_project_bodies = sa.Table(
    "funding_project_bodies",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("funding_project_id", UUID(as_uuid=True), sa.ForeignKey("funding_projects.id"), nullable=False),
    sa.Column("funding_subproject_id", UUID(as_uuid=True), sa.ForeignKey("funding_subprojects.id")),
    sa.Column("body_category", sa.Text),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("representative", sa.Text),
    sa.Column("address", sa.Text),
    sa.Column("telephone", sa.Text),
    sa.Column("email", sa.Text),
    sa.Column("fax", sa.Text),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id")),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

funding_project_participations = sa.Table(
    "funding_project_participations",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column(
        "funding_project_id",
        UUID(as_uuid=True),
        sa.ForeignKey("funding_projects.id"),
        nullable=False,
    ),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id"), nullable=False),
    sa.Column("role", sa.Text, nullable=False),
    sa.Column("link_method", sa.Text, nullable=False),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False),
    sa.Column("evidence", JSONB, nullable=False, server_default="{}"),
    sa.Column("review_status", sa.Text, nullable=False, server_default="AUTO_ACCEPTED"),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

funding_geographic_allocations = sa.Table(
    "funding_geographic_allocations",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("funding_project_id", UUID(as_uuid=True), sa.ForeignKey("funding_projects.id"), nullable=False),
    sa.Column("region", sa.Text),
    sa.Column("prefecture", sa.Text),
    sa.Column("municipality", sa.Text),
    sa.Column("region_code", sa.Text),
    sa.Column("prefecture_code", sa.Text),
    sa.Column("municipality_code", sa.Text),
    sa.Column("amount", sa.Numeric(20, 2)),
    sa.Column("percentage", sa.Numeric(7, 3)),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

funding_links = sa.Table(
    "funding_links",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False),
    sa.Column("funding_project_id", UUID(as_uuid=True), sa.ForeignKey("funding_projects.id"), nullable=False),
    sa.Column("link_method", sa.Text, nullable=False),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False),
    sa.Column("evidence", JSONB, nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    # NULL = not yet reviewed — the pending-review state for a Level 4
    # (§19.2) low-confidence match; mirrors act_links.reviewed_by.
    sa.Column("reviewed_by", UUID(as_uuid=True)),
    sa.Column("review_status", sa.Text, nullable=False, server_default="AUTO_ACCEPTED"),
    sa.Column("reviewed_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("review_notes", sa.Text),
)

procurement_signals = sa.Table(
    "procurement_signals",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("signal_type", sa.Text, nullable=False),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("description", sa.Text),
    sa.Column("buyer_entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id")),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id"), nullable=False),
    sa.Column("source_url", sa.Text),
    sa.Column("source_identifier", sa.Text),
    sa.Column("publication_date", sa.Date),
    sa.Column("expected_notice_date", sa.Date),
    sa.Column("estimated_value", sa.Numeric(20, 2)),
    sa.Column("currency", sa.CHAR(3), nullable=False, server_default="EUR"),
    sa.Column("cpv_codes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("nuts_codes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False),
    sa.Column("evidence", JSONB, nullable=False, server_default=sa.text("'{}'::jsonb")),
    sa.Column("linked_process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id")),
    sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

ted_notice_details = sa.Table(
    "ted_notice_details",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False, unique=True),
    sa.Column("ted_notice_id", sa.Text, nullable=False),
    sa.Column("publication_number", sa.Text),
    sa.Column("raw_format", sa.Text, nullable=False),
    sa.Column("notice_type", sa.Text),
    sa.Column("eforms_version", sa.Text),
    sa.Column("parser_version", sa.Text, nullable=False),
    sa.Column("parse_confidence", sa.Numeric(5, 4), nullable=False, server_default="1"),
    sa.Column("buyer_raw", JSONB),
    sa.Column("supplier_raw", JSONB),
    sa.Column("lots", JSONB),
    sa.Column("country_code", sa.CHAR(2)),
    sa.Column("nuts_codes", sa.ARRAY(sa.Text)),
    sa.Column("related_notice_ids", sa.ARRAY(sa.Text)),
    sa.Column("procedure_identifier", sa.Text),
    sa.Column("notice_version", sa.Text),
    sa.Column("sdk_customization_id", sa.Text),
    sa.Column("previous_notice_ids", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("change_notice_version_identifier", sa.Text),
    sa.Column("is_latest_version", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("estimated_value", sa.Numeric(20, 2)),
    sa.Column("awarded_value", sa.Numeric(20, 2)),
    sa.Column("currency", sa.CHAR(3), server_default="EUR"),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
)

mef_organizations = sa.Table(
    "mef_organizations",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id")),
    sa.Column("source_native_id", sa.Text),
    sa.Column("name", sa.Text),
    sa.Column("afm_raw", sa.Text),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

mef_expenses = sa.Table(
    "mef_expenses",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("mef_organization_id", UUID(as_uuid=True), sa.ForeignKey("mef_organizations.id"), nullable=False),
    sa.Column("recipient_entity_id", UUID(as_uuid=True), sa.ForeignKey("entities.id")),
    sa.Column("recipient_afm_raw", sa.Text),
    sa.Column("amount", sa.Numeric(20, 2)),
    sa.Column("vat_amount", sa.Numeric(20, 2)),
    sa.Column("expense_date", sa.Date),
    sa.Column("related_ada_raw", sa.Text),
    sa.Column("linked_act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id")),
    sa.Column("link_method", sa.Text),
    sa.Column("confidence", sa.Numeric(5, 4)),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

act_links = sa.Table(
    "act_links",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("from_act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False),
    sa.Column("to_act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False),
    sa.Column("link_type", sa.Text, nullable=False),
    sa.Column("link_method", sa.Text, nullable=False),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False),
    sa.Column("evidence", JSONB, nullable=False),
    sa.Column("created_by", sa.Text, nullable=False),
    sa.Column("reviewed_by", UUID(as_uuid=True)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

process_members = sa.Table(
    "process_members",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id"), nullable=False),
    sa.Column("act_id", UUID(as_uuid=True), sa.ForeignKey("procurement_acts.id"), nullable=False),
    sa.Column("added_via", sa.Text, nullable=False),
    sa.Column("added_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

process_merge_log = sa.Table(
    "process_merge_log",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("surviving_process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id"), nullable=False),
    sa.Column("merged_process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id"), nullable=False),
    sa.Column("merge_reason", sa.Text, nullable=False),
    sa.Column("evidence", JSONB, nullable=False),
    sa.Column("performed_by", sa.Text, nullable=False),
    sa.Column("performed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("reverted_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("reverted_by", sa.Text),
)

tenants = sa.Table(
    "tenants",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("plan", sa.Text, nullable=False, server_default="STARTER"),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

users = sa.Table(
    "users",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("email", sa.Text, nullable=False),
    sa.Column("display_name", sa.Text),
    sa.Column("is_internal_reviewer", sa.Boolean, nullable=False, server_default=sa.false()),
    sa.Column("mfa_enabled", sa.Boolean, nullable=False, server_default=sa.false()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

tenant_memberships = sa.Table(
    "tenant_memberships",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("role", sa.Text, nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

api_keys = sa.Table(
    "api_keys",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("name", sa.Text, nullable=False, server_default="API key"),
    sa.Column("key_prefix", sa.Text, nullable=False),
    sa.Column("key_hash", sa.Text, nullable=False),
    sa.Column("scopes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("expires_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("last_used_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("revoked_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

tenant_invitations = sa.Table(
    "tenant_invitations",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("email", sa.Text, nullable=False),
    sa.Column("role", sa.Text, nullable=False),
    sa.Column("token_hash", sa.Text, nullable=False, unique=True),
    sa.Column("invited_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("expires_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("accepted_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("revoked_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

audit_log = sa.Table(
    "audit_log",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id")),
    sa.Column("actor_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("action", sa.Text, nullable=False),
    sa.Column("object_type", sa.Text),
    sa.Column("object_id", UUID(as_uuid=True)),
    sa.Column("details", JSONB),
    sa.Column("ip_address", sa.Text),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

saved_searches = sa.Table(
    "saved_searches",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("query", JSONB, nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

business_profiles = sa.Table(
    "business_profiles",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False, unique=True),
    sa.Column("company_name", sa.Text),
    sa.Column("description", sa.Text, nullable=False, server_default=""),
    sa.Column("cpv_prefixes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("keywords", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("excluded_cpv_prefixes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("excluded_keywords", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("nuts_codes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("municipality", sa.Text),
    sa.Column("buyer_types", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("procedure_types", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("amount_min", sa.Numeric(18, 2)),
    sa.Column("amount_max", sa.Numeric(18, 2)),
    sa.Column("classification_version", sa.Integer, nullable=False, server_default="1"),
    sa.Column("classified_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

business_profile_terms = sa.Table(
    "business_profile_terms",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("profile_id", UUID(as_uuid=True), sa.ForeignKey("business_profiles.id"), nullable=False),
    sa.Column("term_type", sa.Text, nullable=False),
    sa.Column("value", sa.Text, nullable=False),
    sa.Column("label", sa.Text, nullable=False),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False),
    sa.Column("reason", sa.Text, nullable=False),
    sa.Column("source", sa.Text, nullable=False, server_default="RULE"),
    sa.Column("is_active", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

opportunity_relevance_feedback = sa.Table(
    "opportunity_relevance_feedback",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column(
        "process_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_processes.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("profile_version", sa.Integer, nullable=False, server_default="1"),
    sa.Column("label", sa.Text, nullable=False),
    sa.Column("reason", sa.Text),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

bid_workspaces = sa.Table(
    "bid_workspaces",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "process_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_processes.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("owner_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("status", sa.Text, nullable=False, server_default="QUALIFYING"),
    sa.Column("decision", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("decision_rationale", sa.Text),
    sa.Column("decision_by", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("decision_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("submission_due_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

bid_tasks = sa.Table(
    "bid_tasks",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column(
        "bid_workspace_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_workspaces.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("description", sa.Text),
    sa.Column("assigned_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("status", sa.Text, nullable=False, server_default="TODO"),
    sa.Column("priority", sa.Text, nullable=False, server_default="MEDIUM"),
    sa.Column("due_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

bid_requirements = sa.Table(
    "bid_requirements",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column(
        "bid_workspace_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_workspaces.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("requirement_type", sa.Text, nullable=False, server_default="OTHER"),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("description", sa.Text),
    sa.Column("status", sa.Text, nullable=False, server_default="UNREVIEWED"),
    sa.Column("mandatory", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("evidence_document_id", UUID(as_uuid=True), sa.ForeignKey("documents.id", ondelete="SET NULL")),
    sa.Column("evidence_page", sa.Integer),
    sa.Column("source_excerpt", sa.Text),
    sa.Column("owner_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("due_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

bid_comments = sa.Table(
    "bid_comments",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "bid_workspace_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_workspaces.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("task_id", UUID(as_uuid=True), sa.ForeignKey("bid_tasks.id", ondelete="CASCADE")),
    sa.Column(
        "requirement_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_requirements.id", ondelete="CASCADE"),
    ),
    sa.Column("author_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("body", sa.Text, nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

bid_reminders = sa.Table(
    "bid_reminders",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "bid_workspace_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_workspaces.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("task_id", UUID(as_uuid=True), sa.ForeignKey("bid_tasks.id", ondelete="CASCADE")),
    sa.Column(
        "requirement_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_requirements.id", ondelete="CASCADE"),
    ),
    sa.Column("assigned_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("remind_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("channel", sa.Text, nullable=False, server_default="IN_APP"),
    sa.Column("status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("sent_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("attempt_count", sa.Integer, nullable=False, server_default="0"),
    sa.Column("last_attempt_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("next_retry_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("last_error", JSONB),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

tenant_certificates = sa.Table(
    "tenant_certificates",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("certificate_type", sa.Text, nullable=False),
    sa.Column("issuer", sa.Text),
    sa.Column("reference_number", sa.Text),
    sa.Column("file_name", sa.Text),
    sa.Column("storage_uri", sa.Text),
    sa.Column("issued_at", sa.Date),
    sa.Column("expires_at", sa.Date),
    sa.Column("metadata", JSONB, nullable=False, server_default="{}"),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

bid_certificate_links = sa.Table(
    "bid_certificate_links",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "bid_workspace_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_workspaces.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column(
        "requirement_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_requirements.id", ondelete="CASCADE"),
    ),
    sa.Column(
        "certificate_id",
        UUID(as_uuid=True),
        sa.ForeignKey("tenant_certificates.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("linked_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("linked_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

crm_handoffs = sa.Table(
    "crm_handoffs",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "bid_workspace_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_workspaces.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("provider", sa.Text, nullable=False),
    sa.Column("external_reference", sa.Text),
    sa.Column("status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("payload", JSONB, nullable=False),
    sa.Column("response", JSONB),
    sa.Column("error_message", sa.Text),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("synced_at", sa.TIMESTAMP(timezone=True)),
)

notes = sa.Table(
    "notes",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("object_type", sa.Text, nullable=False),
    sa.Column("object_id", UUID(as_uuid=True), nullable=False),
    sa.Column("body", sa.Text, nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

tags = sa.Table(
    "tags",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

tag_links = sa.Table(
    "tag_links",
    metadata,
    sa.Column("tag_id", UUID(as_uuid=True), sa.ForeignKey("tags.id"), primary_key=True),
    sa.Column("object_type", sa.Text, primary_key=True),
    sa.Column("object_id", UUID(as_uuid=True), primary_key=True),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

workspace_watch_items = sa.Table(
    "workspace_watch_items",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("object_type", sa.Text, nullable=False),
    sa.Column("object_id", UUID(as_uuid=True), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

opportunity_pipeline_items = sa.Table(
    "opportunity_pipeline_items",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id"), nullable=False),
    sa.Column("stage", sa.Text, nullable=False, server_default="WATCHING"),
    sa.Column("assigned_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("priority", sa.Text, nullable=False, server_default="MEDIUM"),
    sa.Column("expected_value", sa.Numeric(18, 2)),
    sa.Column("next_action", sa.Text),
    sa.Column("due_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("added_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

opportunity_pipeline_history = sa.Table(
    "opportunity_pipeline_history",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("pipeline_item_id", UUID(as_uuid=True), sa.ForeignKey("opportunity_pipeline_items.id"), nullable=False),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("from_stage", sa.Text),
    sa.Column("to_stage", sa.Text, nullable=False),
    sa.Column("changed_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("changed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

alert_rules = sa.Table(
    "alert_rules",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("event_types", sa.ARRAY(sa.Text), nullable=False),
    sa.Column("filters", JSONB, nullable=False),
    sa.Column("schedule", sa.Text, nullable=False),
    sa.Column("delivery_channels", sa.ARRAY(sa.Text), nullable=False),
    sa.Column("is_active", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("last_evaluated_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("timezone", sa.Text, nullable=False, server_default="Europe/Athens"),
    sa.Column("digest_time", sa.Time, nullable=False, server_default="08:00"),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

opportunity_scores = sa.Table(
    "opportunity_scores",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("process_id", UUID(as_uuid=True), sa.ForeignKey("procurement_processes.id"), nullable=False),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("profile_version", sa.Integer, nullable=False, server_default="1"),
    sa.Column("total_score", sa.Numeric(5, 2), nullable=False),
    sa.Column("cpv_company_fit_score", sa.Numeric(5, 2), nullable=False),
    sa.Column("buyer_affinity_score", sa.Numeric(5, 2), nullable=False),
    sa.Column("timing_score", sa.Numeric(5, 2), nullable=False),
    sa.Column("competitive_attractiveness_score", sa.Numeric(5, 2), nullable=False),
    sa.Column("contract_value_fit_score", sa.Numeric(5, 2), nullable=False),
    sa.Column("data_confidence_score", sa.Numeric(5, 2), nullable=False),
    sa.Column("evidence", JSONB, nullable=False),
    sa.Column("computed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

webhook_deliveries = sa.Table(
    "webhook_deliveries",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("alert_event_id", UUID(as_uuid=True), sa.ForeignKey("alert_events.id"), nullable=False),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("endpoint_url", sa.Text, nullable=False),
    sa.Column("idempotency_key", sa.Text, nullable=False),
    sa.Column("signature", sa.Text, nullable=False),
    sa.Column("request_body", sa.Text),
    sa.Column("status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("attempt_count", sa.Integer, nullable=False, server_default="0"),
    sa.Column("last_attempt_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("next_retry_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("response_status", sa.Integer),
    sa.Column("last_error", JSONB),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

mart_refresh_state = sa.Table(
    "mart_refresh_state",
    metadata,
    sa.Column("mart_name", sa.Text, primary_key=True),
    sa.Column("last_refresh_started_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("last_refresh_finished_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("last_error", JSONB),
)

alert_delivery_targets = sa.Table(
    "alert_delivery_targets",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("alert_rule_id", UUID(as_uuid=True), sa.ForeignKey("alert_rules.id"), nullable=False),
    sa.Column("channel_type", sa.Text, nullable=False),
    sa.Column("target", sa.Text, nullable=False),
    sa.Column("secret", sa.Text),
    sa.Column("is_active", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

alert_events = sa.Table(
    "alert_events",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("alert_rule_id", UUID(as_uuid=True), sa.ForeignKey("alert_rules.id"), nullable=False),
    sa.Column("canonical_object_type", sa.Text, nullable=False),
    sa.Column("canonical_object_id", UUID(as_uuid=True), nullable=False),
    sa.Column("event_type", sa.Text, nullable=False),
    sa.Column("material_change_hash", sa.Text, nullable=False),
    sa.Column("payload", JSONB, nullable=False),
    sa.Column("triggered_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("delivered_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("read_at", sa.TIMESTAMP(timezone=True)),
)

alert_digest_runs = sa.Table(
    "alert_digest_runs",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("alert_rule_id", UUID(as_uuid=True), sa.ForeignKey("alert_rules.id")),
    sa.Column("schedule", sa.Text, nullable=False),
    sa.Column("period_key", sa.Text, nullable=False),
    sa.Column("period_started_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("period_ended_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("event_count", sa.Integer, nullable=False, server_default="0"),
    sa.Column("status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("channels", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("error", JSONB),
    sa.Column("attempt_count", sa.Integer, nullable=False, server_default="0"),
    sa.Column("last_attempt_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("next_retry_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("delivered_at", sa.TIMESTAMP(timezone=True)),
)

export_jobs = sa.Table(
    "export_jobs",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("export_type", sa.Text, nullable=False),
    sa.Column("format", sa.Text, nullable=False),
    sa.Column("filters", JSONB, nullable=False, server_default="{}"),
    sa.Column("status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("row_count", sa.Integer),
    sa.Column("file_name", sa.Text),
    sa.Column("mime_type", sa.Text),
    sa.Column("storage_path", sa.Text),
    sa.Column("error", JSONB),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("started_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("finished_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("expires_at", sa.TIMESTAMP(timezone=True)),
)

opportunity_score_jobs = sa.Table(
    "opportunity_score_jobs",
    metadata,
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), primary_key=True),
    sa.Column("status", sa.Text, nullable=False, server_default="QUEUED"),
    sa.Column("reason", sa.Text, nullable=False),
    sa.Column("requested_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("started_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("finished_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("error", JSONB),
)

onboarding_sessions = sa.Table(
    "onboarding_sessions",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("business_profile_id", UUID(as_uuid=True), sa.ForeignKey("business_profiles.id")),
    sa.Column("status", sa.Text, nullable=False, server_default="DRAFT"),
    sa.Column("current_step", sa.Text, nullable=False, server_default="COMPANY"),
    sa.Column("company_description", sa.Text, nullable=False, server_default=""),
    sa.Column("cpv_suggestions", JSONB, nullable=False, server_default="[]"),
    sa.Column("selected_cpv_codes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("selected_keywords", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("selected_nuts_codes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("initial_opportunity_ids", sa.ARRAY(UUID(as_uuid=True)), nullable=False, server_default="{}"),
    sa.Column("quality_score", sa.Numeric(5, 2)),
    sa.Column("quality_findings", JSONB, nullable=False, server_default="[]"),
    sa.Column("started_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("completed_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

profile_review_requests = sa.Table(
    "profile_review_requests",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "onboarding_session_id",
        UUID(as_uuid=True),
        sa.ForeignKey("onboarding_sessions.id", ondelete="CASCADE"),
    ),
    sa.Column(
        "business_profile_id",
        UUID(as_uuid=True),
        sa.ForeignKey("business_profiles.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("requested_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("assigned_to", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("status", sa.Text, nullable=False, server_default="OPEN"),
    sa.Column("priority", sa.Text, nullable=False, server_default="NORMAL"),
    sa.Column("request_notes", sa.Text),
    sa.Column("reviewer_notes", sa.Text),
    sa.Column("recommended_changes", JSONB, nullable=False, server_default="{}"),
    sa.Column("requested_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("reviewed_at", sa.TIMESTAMP(timezone=True)),
)

decision_makers = sa.Table(
    "decision_makers",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column(
        "buyer_entity_id",
        UUID(as_uuid=True),
        sa.ForeignKey("entities.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("full_name", sa.Text, nullable=False),
    sa.Column("job_title", sa.Text),
    sa.Column("department", sa.Text),
    sa.Column("decision_role", sa.Text, nullable=False, server_default="STAKEHOLDER"),
    sa.Column("email", sa.Text),
    sa.Column("phone", sa.Text),
    sa.Column("profile_url", sa.Text),
    sa.Column("source_system", sa.Text, nullable=False),
    sa.Column("source_url", sa.Text),
    sa.Column("source_identifier", sa.Text),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("legal_basis", sa.Text, nullable=False, server_default="PUBLIC_OFFICIAL_RECORD"),
    sa.Column("confidence", sa.Numeric(5, 4), nullable=False, server_default="0.5"),
    sa.Column("active_from", sa.TIMESTAMP(timezone=True)),
    sa.Column("active_until", sa.TIMESTAMP(timezone=True)),
    sa.Column("is_current", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("evidence", JSONB, nullable=False, server_default="{}"),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

decision_maker_watches = sa.Table(
    "decision_maker_watches",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column(
        "decision_maker_id",
        UUID(as_uuid=True),
        sa.ForeignKey("decision_makers.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("notes", sa.Text),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

framework_supplier_memberships = sa.Table(
    "framework_supplier_memberships",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column(
        "framework_act_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_acts.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column(
        "supplier_entity_id",
        UUID(as_uuid=True),
        sa.ForeignKey("entities.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("lot_identifier", sa.Text),
    sa.Column("membership_status", sa.Text, nullable=False, server_default="ACTIVE"),
    sa.Column("awarded_value", sa.Numeric(20, 2)),
    sa.Column("valid_from", sa.Date),
    sa.Column("valid_until", sa.Date),
    sa.Column("source_record_id", UUID(as_uuid=True), sa.ForeignKey("source_records.id")),
    sa.Column("evidence", JSONB, nullable=False, server_default="{}"),
    sa.Column("observed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

framework_watches = sa.Table(
    "framework_watches",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column(
        "framework_act_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_acts.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("notify_before_days", sa.Integer, nullable=False, server_default="90"),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

tenant_bid_content = sa.Table(
    "tenant_bid_content",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("content_type", sa.Text, nullable=False),
    sa.Column("body", sa.Text, nullable=False),
    sa.Column("tags", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("source_file_name", sa.Text),
    sa.Column("source_uri", sa.Text),
    sa.Column("approved", sa.Boolean, nullable=False, server_default=sa.false()),
    sa.Column("approved_by", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("approved_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

proposal_sections = sa.Table(
    "proposal_sections",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "bid_workspace_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_workspaces.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("requirement_id", UUID(as_uuid=True), sa.ForeignKey("bid_requirements.id", ondelete="SET NULL")),
    sa.Column("section_key", sa.Text, nullable=False),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("display_order", sa.Integer, nullable=False, server_default="0"),
    sa.Column("status", sa.Text, nullable=False, server_default="DRAFT"),
    sa.Column("current_version", sa.Integer, nullable=False, server_default="1"),
    sa.Column("body", sa.Text, nullable=False, server_default=""),
    sa.Column("citations", JSONB, nullable=False, server_default="[]"),
    sa.Column("generation_metadata", JSONB, nullable=False, server_default="{}"),
    sa.Column("assigned_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

proposal_section_versions = sa.Table(
    "proposal_section_versions",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "proposal_section_id",
        UUID(as_uuid=True),
        sa.ForeignKey("proposal_sections.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("version_number", sa.Integer, nullable=False),
    sa.Column("body", sa.Text, nullable=False),
    sa.Column("citations", JSONB, nullable=False, server_default="[]"),
    sa.Column("change_summary", sa.Text),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

proposal_exports = sa.Table(
    "proposal_exports",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "bid_workspace_id",
        UUID(as_uuid=True),
        sa.ForeignKey("bid_workspaces.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column("format", sa.Text, nullable=False),
    sa.Column("status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("file_name", sa.Text),
    sa.Column("storage_path", sa.Text),
    sa.Column("manifest", JSONB, nullable=False, server_default="{}"),
    sa.Column("error", JSONB),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("finished_at", sa.TIMESTAMP(timezone=True)),
)

saas_plans = sa.Table(
    "saas_plans",
    metadata,
    sa.Column("code", sa.Text, primary_key=True),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("description", sa.Text, nullable=False),
    sa.Column("monthly_price_cents", sa.Integer),
    sa.Column("annual_price_cents", sa.Integer),
    sa.Column("currency", sa.Text, nullable=False, server_default="EUR"),
    sa.Column("trial_days", sa.Integer, nullable=False, server_default="14"),
    sa.Column("entitlements", JSONB, nullable=False),
    sa.Column("is_public", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("display_order", sa.Integer, nullable=False, server_default="0"),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

tenant_subscriptions = sa.Table(
    "tenant_subscriptions",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("plan_code", sa.Text, sa.ForeignKey("saas_plans.code"), nullable=False),
    sa.Column("status", sa.Text, nullable=False, server_default="TRIALING"),
    sa.Column("billing_provider", sa.Text, nullable=False, server_default="MANUAL"),
    sa.Column("provider_customer_id", sa.Text),
    sa.Column("provider_subscription_id", sa.Text),
    sa.Column("trial_started_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("trial_ends_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("current_period_start", sa.TIMESTAMP(timezone=True)),
    sa.Column("current_period_end", sa.TIMESTAMP(timezone=True)),
    sa.Column("cancel_at_period_end", sa.Boolean, nullable=False, server_default=sa.false()),
    sa.Column("entitlements_override", JSONB, nullable=False, server_default="{}"),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

entitlement_usage = sa.Table(
    "entitlement_usage",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("metric_code", sa.Text, nullable=False),
    sa.Column("period_start", sa.Date, nullable=False),
    sa.Column("period_end", sa.Date, nullable=False),
    sa.Column("usage_count", sa.BigInteger, nullable=False, server_default="0"),
    sa.Column("last_incremented_at", sa.TIMESTAMP(timezone=True)),
)

support_tickets = sa.Table(
    "support_tickets",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("created_by", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("assigned_to", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("subject", sa.Text, nullable=False),
    sa.Column("description", sa.Text, nullable=False),
    sa.Column("category", sa.Text, nullable=False, server_default="PRODUCT"),
    sa.Column("priority", sa.Text, nullable=False, server_default="NORMAL"),
    sa.Column("status", sa.Text, nullable=False, server_default="OPEN"),
    sa.Column("response_due_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("resolved_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

account_success_tasks = sa.Table(
    "account_success_tasks",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("owner_user_id", UUID(as_uuid=True), sa.ForeignKey("users.id")),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("description", sa.Text),
    sa.Column("status", sa.Text, nullable=False, server_default="OPEN"),
    sa.Column("due_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("completed_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

service_incidents = sa.Table(
    "service_incidents",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("title", sa.Text, nullable=False),
    sa.Column("affected_components", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("severity", sa.Text, nullable=False),
    sa.Column("status", sa.Text, nullable=False),
    sa.Column("public_message", sa.Text, nullable=False),
    sa.Column("started_at", sa.TIMESTAMP(timezone=True), nullable=False),
    sa.Column("resolved_at", sa.TIMESTAMP(timezone=True)),
    sa.Column("updates", JSONB, nullable=False, server_default="[]"),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

oidc_subject_tenant_bindings = sa.Table(
    "oidc_subject_tenant_bindings",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("issuer", sa.Text, nullable=False),
    sa.Column("subject", sa.Text, nullable=False),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("last_login_at", sa.TIMESTAMP(timezone=True)),
)

sector_profile_templates = sa.Table(
    "sector_profile_templates",
    metadata,
    sa.Column("code", sa.Text, primary_key=True),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("description", sa.Text, nullable=False),
    sa.Column("cpv_prefixes", sa.ARRAY(sa.Text), nullable=False),
    sa.Column("keywords", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("excluded_keywords", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("recommended_alerts", JSONB, nullable=False, server_default="[]"),
    sa.Column("is_active", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("display_order", sa.Integer, nullable=False, server_default="0"),
)

document_phrase_monitors = sa.Table(
    "document_phrase_monitors",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("name", sa.Text, nullable=False),
    sa.Column("phrases", sa.ARRAY(sa.Text), nullable=False),
    sa.Column("match_mode", sa.Text, nullable=False, server_default="ANY"),
    sa.Column("cpv_prefixes", sa.ARRAY(sa.Text), nullable=False, server_default="{}"),
    sa.Column("is_active", sa.Boolean, nullable=False, server_default=sa.true()),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("updated_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

document_phrase_matches = sa.Table(
    "document_phrase_matches",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "monitor_id",
        UUID(as_uuid=True),
        sa.ForeignKey("document_phrase_monitors.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column(
        "document_id",
        UUID(as_uuid=True),
        sa.ForeignKey("documents.id", ondelete="CASCADE"),
        nullable=False,
    ),
    sa.Column(
        "process_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_processes.id", ondelete="CASCADE"),
    ),
    sa.Column("matched_phrases", sa.ARRAY(sa.Text), nullable=False),
    sa.Column("page_numbers", sa.ARRAY(sa.Integer), nullable=False, server_default="{}"),
    sa.Column("excerpts", JSONB, nullable=False, server_default="[]"),
    sa.Column("matched_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

document_transformation_jobs = sa.Table(
    "document_transformation_jobs",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column("user_id", UUID(as_uuid=True), sa.ForeignKey("users.id"), nullable=False),
    sa.Column("transformation_type", sa.Text, nullable=False),
    sa.Column("document_ids", sa.ARRAY(UUID(as_uuid=True)), nullable=False),
    sa.Column("status", sa.Text, nullable=False, server_default="PENDING"),
    sa.Column("file_name", sa.Text),
    sa.Column("storage_path", sa.Text),
    sa.Column("error", JSONB),
    sa.Column("created_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
    sa.Column("finished_at", sa.TIMESTAMP(timezone=True)),
)

eu_benchmark_snapshots = sa.Table(
    "eu_benchmark_snapshots",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("snapshot_date", sa.Date, nullable=False),
    sa.Column("date_from", sa.Date, nullable=False),
    sa.Column("date_to", sa.Date, nullable=False),
    sa.Column("cpv_prefix", sa.Text, nullable=False),
    sa.Column("country_code", sa.Text, nullable=False),
    sa.Column("notice_count", sa.Integer, nullable=False),
    sa.Column("award_count", sa.Integer, nullable=False),
    sa.Column("total_value", sa.Numeric(24, 2), nullable=False, server_default="0"),
    sa.Column("median_value", sa.Numeric(24, 2)),
    sa.Column("average_bidders", sa.Numeric(10, 3)),
    sa.Column("sme_award_rate", sa.Numeric(7, 4)),
    sa.Column("single_bid_rate", sa.Numeric(7, 4)),
    sa.Column("dimensions", JSONB, nullable=False, server_default="{}"),
    sa.Column("computed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)

tenant_cross_border_matches = sa.Table(
    "tenant_cross_border_matches",
    metadata,
    sa.Column("id", UUID(as_uuid=True), primary_key=True),
    sa.Column("tenant_id", UUID(as_uuid=True), sa.ForeignKey("tenants.id"), nullable=False),
    sa.Column(
        "process_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_processes.id", ondelete="CASCADE"),
    ),
    sa.Column(
        "act_id",
        UUID(as_uuid=True),
        sa.ForeignKey("procurement_acts.id", ondelete="CASCADE"),
    ),
    sa.Column("profile_version", sa.Integer, nullable=False),
    sa.Column("country_code", sa.Text, nullable=False),
    sa.Column("match_score", sa.Numeric(5, 2), nullable=False),
    sa.Column("reasons", JSONB, nullable=False, server_default="[]"),
    sa.Column("barriers", JSONB, nullable=False, server_default="[]"),
    sa.Column("computed_at", sa.TIMESTAMP(timezone=True), nullable=False, server_default=sa.func.now()),
)
