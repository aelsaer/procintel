# packages/observability

OpenTelemetry tracing, Prometheus metrics, structured JSON logging helpers
shared across services (spec §11 observability stack), plus the per-source
freshness/health dashboard primitives referenced in the ingestion Definition
of Done (spec §46).
