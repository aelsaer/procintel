# docs/runbooks

Operational runbooks: responding to a source outage (degraded mode per spec
§36), handling schema-drift quarantine, reviewing entity-merge conflicts,
rotating API keys, replaying a partition from the raw zone. To be written
alongside Στάδιο 1+ implementation.
