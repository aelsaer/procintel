# tests/golden_records

Hand-verified end-to-end expected outputs (a known ΑΔΑΜ chain resolved to a full
`procurement_360` record) used to catch regressions in linkage/entity-resolution
logic across the whole pipeline, not just one connector.
