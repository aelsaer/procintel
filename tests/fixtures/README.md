# tests/fixtures

Recorded sample payloads per source/resource (raw JSON/XML as returned by each
API), the Στάδιο 0 deliverable in spec §44. Used by contract tests and for
offline connector development without hitting live rate limits.
